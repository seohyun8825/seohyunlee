{
  "name": "seohyun-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Seohyun Lee – static portfolio site for GitHub Pages (project site).",
  "scripts": {
    "deploy": "gh-pages -d .",
    "publish": "git add . && git commit -m \"Auto update\" && git push origin main && npm run deploy",
    "deploy-simple": "git add . && git commit -m \"Auto update\" && git push origin main && git checkout gh-pages && git merge main && git push origin gh-pages && git checkout main",
    "dev": "python -m http.server 8000"
  },
  "main": "script.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
